<Window x:Class="AffirmationBar.WPF.Views.StudentInfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:cv="clr-namespace:AffirmationBar.WPF.Converters"
        mc:Ignorable="d"
        Title="{Binding Names}" Height="500" Width="500" >

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="../Resources/bckgr.jpg" />
        </Grid.Background>

        <StackPanel>

            <ComboBox  x:Name="txtReason"  HorizontalAlignment="Center" VerticalAlignment="Center" ItemsSource ="{Binding Reason}" ItemStringFormat="Да послужи пред: {0}"  VerticalContentAlignment="Center"  Foreground="#FF171313" Width="409" Height="41">
                <ComboBoxItem Content="БДЖ"/>
                <ComboBoxItem Content="ПСБО"/>
                <ComboBoxItem Content="Градски транспорт"/>
                <ComboBoxItem Content="НОИ"/>
            </ComboBox>

            <Button x:Name="getDoc" Content="Вземи уверение" HorizontalAlignment="Center" VerticalAlignment="Center" Click="getDoc_Click" />

        </StackPanel>
    </Grid>
</Window>
